# Splunk Connection Configuration

# Splunk server hostname or IP
SPLUNK_HOST=localhost

# Splunk management port (default: 8089)
SPLUNK_PORT=8089

# Connection scheme (http or https)
SPLUNK_SCHEME=https

# Authentication Method 1: Token-based (recommended)
# SPLUNK_TOKEN=your-splunk-token-here

# Authentication Method 2: Username/Password (if not using token)
# SPLUNK_USERNAME=your-username
# SPLUNK_PASSWORD=your-password

# Note: If SPLUNK_TOKEN is set, it will be used. Otherwise, username/password will be used.
